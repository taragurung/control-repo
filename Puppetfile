#mod 'apache',
#:git    => 'https://github.com/puppetlabs/puppetlabs-apache',
#:branch => 'proxy_match'

mod 'puppetlabs/concat',
  :git    => 'https://github.com/puppetlabs/puppetlabs-apache',
  :branch => 'proxy_match'


#similarly you can specify what modules to install and on which branch which is the environment for us
#Syntax
#modulename, git repo name  followed by branch (which is also the corresponding environment for us)

